"""Reference graphs.

These are runnable and test-covered, but nothing in the product calls them yet.
They exist to show the shape of a real graph — a bounded revision loop, a
conditional branch, a human approval gate — without any caller having to guess
from the API docs.
"""
