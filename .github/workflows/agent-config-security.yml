name: Agent Config Security

# DISABLED 2026-05-26: repo-level GHA infra failure since 2026-05-24T20:00Z.
# Every PR/push run completes in <5s with conclusion=failure (pre-checkout
# startup failure, not a step failing). Restore by re-adding `pull_request`
# and `push` triggers below once GH Actions billing/quota/runner permissions
# are confirmed healthy. AgentShield baseline already regenerated on Linux
# in commit efabd0cc — gate passes locally via `npm run agent-config:scan`.
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  scan:
    name: AgentShield scan
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run pinned AgentShield scan
        run: npm run agent-config:scan
