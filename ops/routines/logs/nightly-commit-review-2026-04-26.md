# Nightly Commit Review — 2026-04-26

**Window:** last 24 hours (2026-04-25 → 2026-04-26)
**Commits reviewed:** 31 (including merge commits and branch duplicates)
**Issues created:** 2
**AUTO-FIXES applied:** 0 (no LOW-risk bugs found warranting a fix)

---

## Commit Triage

### HIGH risk
| SHA | Description | Finding |
|-----|-------------|---------|
| `164d21b` | feat(fraud): disposable email block, velocity limits, CC mismatch | **2 bugs filed** — see issues #93, #94 |
| `c7c74f1` | feat(onboarding): wire wizard route, Stripe success_url | Clean — Stripe success_url routing is correct; source field optional and safe |
| `475513b` / `1cea985` / `a7248a5` | Merge: refactor/auth-helpers-to-dependencies (PR #92) | Auth helper refactor — already reviewed via PR; no new issues spotted |
| `b700ac2` | chore(ai): dependencies.py update + router imports | Auth helper centralisation — consistent pattern, no issues |

### MEDIUM risk
| SHA | Description | Finding |
|-----|-------------|---------|
| `5dbca8e` / `2e5d3d2` | auth.py minor changes + OnboardingWizardPage + WizardStepAutoKB | Auth.py change is small scope guard addition — clean. New wizard step is frontend only. |
| `4fee5cf` / `9e4f4d0` | migration 112_widget_configs_onboarding_v2.sql + schema-log | Migration adds columns to widget_configs; RLS not changed; indexes present — clean |
| `724c8a6` / `789166c` | onboarding.py + vertical_presets.py + test_auto_kb_v2.py | New service + 230-line test file (13 tests). No schema mutations. Clean. |

### LOW risk
| SHA | Description | Finding |
|-----|-------------|---------|
| `5465288` / `c628876` | chore(ai): 34 router import updates (auth-helpers migration) | Mass mechanical import swap — consistent, no logic change. Clean. |
| `62f8722` | fix(onboarding): BillingPage toast, mobile tap targets, CSP verify | UI-only. Clean. |
| `6eb1d1c` | docs: auto-log bug fix from 62f8722 | Docs only. Clean. |
| `c5caa31` | subconscious: wire check_project_invariants.py into pre-commit | Subconscious run — docs/config only. Clean. |
| `3b2df1f` / `763fa21` | feat(skills): proactive triggers in skillify | Skill file update. Clean. |
| `43faf0f` / `2b96f1e` | docs(kb): SMB workflow framework | KB raw docs. Clean. |
| `6c5c733` / `1df2d93` | docs(kb): naming gap in automation_engine | 1-line doc fix. Clean. |
| `a38c249` / `ba41741` | chore(ai): repo evaluations KB entry | KB raw docs. Clean. |
| `8aa2dff` | chore(ai): memory-tiered-retrieval rule + karpathy skill addition | Rule/skill docs. Clean. |
| `7c1c51b` | chore(ai): settings.local.json update | Config. Clean. |
| `118880e` | docs(kb): repo evaluations 2026-04-25 | KB docs. Clean. |
| Merge commits | PR #87, #88, #92 merges | No code, clean. |

---

## Issues Filed

### #93 — HIGH — billing
**`guard_checkout_for_fraud` flags `no_payment_required` as fraud — breaks coupon/trial signups**
- File: `backend/services/fraud_guard.py:121-123`
- `payment_status != "paid"` check also matches `"no_payment_required"` → tenant paused on legitimate coupon signup
- Fix: skip fraud check (return None) when `payment_status == "no_payment_required"`
- https://github.com/aferna6-cell/agentnexlify/issues/93

### #94 — MEDIUM — billing
**`IndexError` crash when `charges.data` is empty list**
- File: `backend/services/fraud_guard.py:135,141-147`
- `pi.get("charges", {}).get("data", [{}])[0]` — default `[{}]` doesn't protect against `data = []`
- Outside the try/except block → propagates to webhook handler → 500 + Stripe retries
- Fix: extract `charge = (charges_data[0] if (charges_data := pi.get("charges",{}).get("data") or []) else {})`
- https://github.com/aferna6-cell/agentnexlify/issues/94

---

## Notable Patterns

- **Auth helpers refactor (PR #92)** landed cleanly. 34 routers updated mechanically. `dependencies.py` is the new canonical home for `get_current_tenant` et al.
- **Fraud guard** is the only source of new bugs this cycle. Both bugs are in the same file and function — recommend fixing together in one PR.
- **Duplicate commit SHAs** in the log (e.g., `5465288` / `c628876` with identical stats) are merge artifacts from the branch history being replayed into main. Not a code issue.
- **Migration 113** (signup_attempts) and **Migration 112** (widget_configs) both landed with proper RLS and indexes. No schema invariant violations.

---

## CLAUDE.md Invariant Check

- `client_id` vs `tenant_id`: fraud_guard.py uses `tenant_id` in the `signup_attempts` insert — this is the `tenants.id` column (correct usage; `client_id` rule applies to `leads`/`conversations` tables only). ✓
- No `from __future__ import annotations` in any new FastAPI files. ✓
- Widget JS: no widget changes this cycle. ✓
- Secrets: none detected in new commits. ✓

---

*Generated by nightly-commit-review agent. Next run: 2026-04-27.*
