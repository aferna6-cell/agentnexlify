# Nightly Commit Review — 2026-07-24 (UTC)

Generated by automated nightly-commit-review routine.

---

## Commits Reviewed (last 24h)

| SHA | Message | Risk | Verdict |
|-----|---------|------|---------|
| e9b4972 | AI-triggered native booking panel via SHOW_BOOKING_PANEL marker | MEDIUM | CLEAN |
| ab1a7c2 | Migration 187 + brain refresh + email_sequences split + booking-link fix | MEDIUM | CLEAN |
| b8663d8 | Close long-running threads — ToS §4, Stripe Connect decision, brain sync | LOW | CLEAN |
| 21889d7 | docs(ops): refine #266 runbook with verified prod encryption state | LOW | CLEAN |
| 8246ff1 | docs(migrations): reconcile 185/186 to APPLIED | LOW | CLEAN |
| 4045bec | docs: update current-tasks with subconscious run 99 status | LOW | CLEAN |
| 2677bbf | subconscious: run 100 (2026-07-23) — Step 9G | LOW | CLEAN |
| c886b09 | chore(deps): bump peter-evans/create-pull-request from 6 to 8 | LOW | CLEAN |
| f1c6244 | chore(deps): bump actions/setup-python from 5 to 7 | LOW | CLEAN |
| 5032680 | chore(deps): bump actions/cache from 4 to 6 | LOW | CLEAN |
| 5d9a872 | chore(deps): bump actions/setup-node from 4 to 7 | LOW | CLEAN |
| 5875043 | chore(deps): bump actions/github-script from 7 to 9 | LOW | CLEAN |
| 5d3093a | ops: morning-digest 2026-07-23 | LOW | CLEAN |
| 94e3c1a | ops: nightly-commit-review 2026-07-23 | LOW | CLEAN |

---

## Detailed Findings

### e9b4972 — AI booking panel trigger (MEDIUM / CLEAN)

**What changed:** New `widget_chat_booking_action.py` module + integration into `widget_chat.py`. Backend now strips `SHOW_BOOKING_PANEL` marker from AI replies and returns `show_booking=True` flag to widget. Widget JS updated across all 3 mirrors.

**Checks passed:**
- Widget byte-identical across `widget/`, `frontend/public/widget/`, `landing-page-v2/widget/` — MD5 `223951235e06544b9cd65f1ca07234cb` (all three) ✓
- No `from __future__ import annotations` in new backend file ✓
- Marker always stripped (never reaches visitor), flag suppressed for booking-disabled tenants ✓
- `WidgetChatResponse.show_booking` defaults `False` — backward-compatible with old widget builds ✓
- Tests in `test_widget_show_booking.py` cover: marker stripped + flag set, passthrough with no marker, suppression on disabled tenant, mid-text marker cleanup ✓
- Schema model addition is additive only ✓

**No bugs found. No action needed.**

### ab1a7c2 — Migration 187 + email_sequences split (MEDIUM / CLEAN)

**What changed:** Migration 187 adds explicit `service_role` policy on `pending_automations` (prior migration 186 created the table with RLS enabled but no policy — safe by default, now explicitly documented). Email sequences god-class split: 1143-line `email_sequences.py` factored into `email_crud.py` (529L), `email_enrollment.py` (328L), `email_processor.py` (341L). Main.py router registrations updated.

**Checks passed:**
- Migration 187 is idempotent (DROP IF EXISTS + CREATE) ✓
- Migration is purely additive — no column or table changes ✓
- Email sequence tables use `tenant_id` (correct per schema — not `leads`/`conversations` tables) ✓
- `leads` table query in `email_enrollment.py:289` correctly uses `.eq("client_id", tenant_id)` ✓
- No `from __future__ import annotations` in new router files ✓
- `main.py` imports and registers all 3 new routers ✓
- 8 test failures are pre-existing auth fixture issues (reproduce on pre-split HEAD), not regressions ✓

**No bugs found. No action needed.**

### b8663d8 — Docs / ToS (LOW / CLEAN)

Minor ToS clause update + Stripe Connect architecture decision doc. Brain open loops updated. No code changes.

### 8246ff1 — Migration reconciliation (LOW / CLEAN)

Documentation-only: confirmed migrations 185/186 are APPLIED to prod. SQL files annotated with application notes. No schema changes.

### GitHub Actions bumps (LOW / CLEAN)

Standard Dependabot version bumps: setup-python 5→7, setup-node 4→7, cache 4→6, github-script 7→9, create-pull-request 6→8. No logic changes.

---

## Schema Discipline Check

- `client_id` rule (leads/conversations): **VERIFIED** — email_enrollment.py line 289 correctly uses `client_id` when querying `leads` ✓
- No `lead_stage`, `service_interest`, or `tenant_id` on leads/conversations tables ✓
- No `from __future__ import annotations` in FastAPI files ✓

---

## Known Open Issues (not introduced this cycle)

- KB autopopulate GitHub Actions workflow dead (#403/#500) — manual workaround used in this batch. Pre-existing.
- 8 pre-existing auth test failures in email_sequences suite — pre-existing, unrelated to the split.

---

## Verdict: NO ISSUES FOUND

No LOW-risk bugs requiring immediate fixes. No MEDIUM/HIGH issues requiring GitHub issues.
All MEDIUM-risk changes are well-tested with proper schema discipline and backward compatibility.

---

_Routine: nightly-commit-review | Run: 2026-07-24T00:00:00Z_
