# Nightly Commit Review — 2026-05-02

**Run time:** 2026-05-02 UTC  
**Commits reviewed:** 9 (last 24h)  
**Issues created:** 2 (MEDIUM)  
**Fixes applied:** 1 (LOW)

---

## Commits Triaged

| SHA | Message | Risk | Verdict |
|-----|---------|------|---------|
| `5b33cb0` | kb(log): append run summary 2026-05-01 18:19 | LOW | Clean — docs only |
| `b5e934b` | chore(ai): auto-commit Claude edits [main 2026-05-01 18:19] | LOW | Clean — KB raw articles + index |
| `fa466ca` | chore(ai): auto-commit Claude edits [main 2026-05-01 18:05] | **MEDIUM** | New email_sequences router — see issues below |
| `ade6c99` | kb: lock Twilio MCP package + off-peak promo verifications | LOW | Clean — KB docs |
| `d7a4401` | chore(ai): auto-commit Claude edits [main 2026-05-01 14:16] | LOW | Clean — plugins.md + example fastmcp server (not wired to prod) |
| `4fdeedc` | docs: automated morning startup 2026-05-01 | LOW | Clean — daily logs + bug-patterns.md |
| `9d94ddc` | ops: morning-digest 2026-05-01 | LOW | Clean — ops log |
| `fd9dc98` | subconscious: run 2026-05-01 (run 12) | LOW | Clean — internal state |
| `b34e687` | ops: nightly-commit-review 2026-05-01 | LOW | Clean — ops log + gitignore |

---

## Issues Created (MEDIUM)

### #112 — N+1 queries in list_enrollments and list_sequences
**File:** `backend/routers/email_sequences.py`  
**Commit:** `fa466ca`

- `list_enrollments` (lines 708–724): 1 DB call per enrollment to fetch lead info. 1000 enrollments = 1001 queries per request.
- `list_sequences` (lines 256–283): 2 DB calls per sequence (step_count + enrollment_count). 50 sequences = 101 queries.

**Suggested fix:** Bulk queries using `.in_()` + group-by aggregation in Python.  
**GH issue:** https://github.com/aferna6-cell/agentnexlify/issues/112

---

### #113 — Code duplication: process_sequences vs run_sequence_processor
**File:** `backend/routers/email_sequences.py`  
**Commit:** `fa466ca`

`process_sequences` (HTTP, lines 805–975) and `run_sequence_processor` (standalone, lines 997–1102) share ~120 lines of near-identical per-send loop logic. Any bug fix needs to be applied twice. Extract into private `_process_pending_sends()` helper.

**GH issue:** https://github.com/aferna6-cell/agentnexlify/issues/113

---

## Fix Applied (LOW)

### Docstring + 409 error accuracy in `_enroll_lead`
**File:** `backend/routers/email_sequences.py`  
**Commit:** `fa466ca`

- **Docstring** (line ~110): stated "Returns None if already enrolled (conflict)" — wrong. Code returns the existing enrollment_id on conflict; None only on database error. Fixed docstring.
- **409 error** (line ~857): `HTTPException(status_code=409, detail="Lead is already enrolled...")` — misleading because `enrollment_id is None` is only reachable on DB error (not duplicate). Changed to `status_code=500, detail="Failed to enroll lead (database error)"`.

Verified: `python3 -c "import ast; ast.parse(open('backend/routers/email_sequences.py').read())"` — PASS

---

## Critical Invariants Check (fa466ca)

| Rule | Status |
|------|--------|
| `client_id` on leads table | PASS — line 763 uses `.eq("client_id", tenant_id)` |
| No `from __future__ import annotations` | PASS — not present |
| Tenant isolation on all routes | PASS — every route scopes by `tenant_id` via `_get_current_tenant` |
| Static routes before param routes | NOTE — `/internal/process-sequences` defined after `/{sequence_id}` param routes. No active shadowing risk (different path depths), but violates api-conventions.md. Noted in log; not critical enough for an issue. |

---

## No Issues Found On

- All KB commits (LOW — markdown only)
- `mcp-servers/example-fastmcp/server.py` (not wired to prod; clean scaffold)
- `AutomationActivityCard.jsx` (1-line addition of `email_sequence_sent` event label — correct)
- `scripts/post_onboarding_v2_phase1_issues.sh` (shell script for issue creation only — no prod impact)

---

*Generated by nightly-commit-review agent.*
