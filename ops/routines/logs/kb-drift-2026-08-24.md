# KB Drift Report — 2026-08-24

## Result: DRIFT DETECTED — PUSH BLOCKED BY PRE-PUSH HOOK

## PRs scanned (last 7 days)

| PR | Title | Merged |
|----|-------|--------|
| #677 | Managed-agents audit + fixes for all 7 findings | 2026-08-24 |
| #678 | fix(agents): stop confirming appointments that do not exist | 2026-08-24 |

## Wiki articles with drift

### `knowledge-base/wiki/ai-llm/anthropic-managed-agents-architecture.md`

**Drift from PR #677 and #678:** The "Relevance to AgentNexLiFy" section described
session budget caps and fail-closed booking verification as aspirational. Both are
now shipped:

- `managed_agents.py` exports `DEFAULT_SESSION_BUDGET_CENTS = 500` and
  `default_session_budget_cents()` applied to all non-interactive sessions.
- `scripts/check_project_invariants.py` enforces this via `check_agent_sessions_are_budgeted()`.
- `appointment_booker.py` now validates each agent-claimed UUID via
  `_appointment_row_exists()` before reporting `status=booked`.

**Fix applied:** Article updated, branch `claude/kb-drift-2026-08-24` committed.

## Push status: BLOCKED

Pre-push hook raised a CRITICAL security flag on `appointment_booker.py`:

> "_appointment_row_exists() queries appointments table (tenant_id column) but is
> called with inp.client_id — possible cross-tenant isolation bypass."

**Assessment: likely false positive.** The PR's own code comment at line 306
explicitly documents `tenant_id=client_id` as intentional: "client_id passed as
session metadata tenant_id". The `appointments` table uses `tenant_id` as column
name while `leads`/`conversations` use `client_id` — both refer to the same
business-tenant entity. CLAUDE.md documents this as a known naming inconsistency
across tables. The same pattern appears at line 386: `tenant_id=inp.client_id`.

## Action required

Human confirmation needed to resolve one of:
1. Confirm false positive → `git push --no-verify` to land the KB article fix.
2. Confirm real bug → open issue to fix `appointment_booker.py` before landing KB fix.

The KB article fix is committed locally on branch `claude/kb-drift-2026-08-24`.
