[project]
name = "agentnexlify-internal-mcp"
version = "0.1.0"
description = "Project-specific MCP server for tenant-aware operations"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
