# Agent OS — environment template. Copy to .env and fill in.
#
# Optional. With no key, the demo uses the deterministic offline provider and
# every agent still drafts grounded, rule-clean output from templates. Set this
# (and `pip install anthropic`) to upgrade drafts to live Claude generation.
ANTHROPIC_API_KEY=
