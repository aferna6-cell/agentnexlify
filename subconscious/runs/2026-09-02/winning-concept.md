# Winning Concept — Run 117 (2026-09-02)

## Recommendation
Add Step 9L or Step 9M (depending on SKILL.md state) to `.claude/skills/nightly-commit-review/SKILL.md` — an ai-ready loop stall diagnostic that detects when 0 PRs opened in 14 days despite stale ai-ready issues, flags "LOOP STALLED", and comments on the oldest stale issue with diagnostic context.

**Bonus (same commit):** Add 60d advisory log to Step 9E (2–3 lines) — earlier warning before the 76d alert threshold.

## Context: Branch State
Branch `subconscious/run-2026-08-30-pm` already has runs 115 (2026-09-01) and 116 (2026-09-01-pm). Run 116 proposed "Step 9L: nightly connector auth pattern scan" (SKILL.md edit, autonomous-executable). This run (117) proposes a DIFFERENT Step 9L — a loop PR output check. Before implementing, the nightly must verify SKILL.md content:
- If Step 9L block absent → add as Step 9L
- If Step 9L already present (from run 116 implementation) → add as Step 9M

## Why This, Why Now
nightly-2026-09-02 Step 9D: 3 stale ai-ready issues (#669, #660, #643) with AUTOPILOT_GH_TOKEN valid at 60d. Step 9D surfaces staleness but does NOT diagnose whether the loop is producing PRs at all. The loop could be stalled for any of: token misconfiguration, label mismatch, GH Action runner issue, Routine substrate failure. No current nightly step checks PR output from the loop. This diagnostic block adds exactly that check.

## Implementation Sketch

### Step 9L/9M block to insert (after Step 9K summary line, before Step 10)

```markdown
### Step 9L — ai-ready Loop Stall Diagnostic
(Use Step 9M if Step 9L already occupied by run 116's connector auth scan)

1. Call `mcp__github__search_pull_requests` with query `repo:aferna6-cell/agentnexlify is:pr head:issue-to-pr- created:>{14d_ago_date}` to find loop-opened PRs.
2. Count results: `loop_prs_14d` = number of matching PRs.
3. Get stale ai-ready issue count from Step 9D output: `stale_ai_ready`.
4. Logic:
   a. If `loop_prs_14d >= 1`:
      - Log: "Step 9L: loop ACTIVE ({loop_prs_14d} PRs in 14d), {stale_ai_ready} stale ai-ready issues"
      - Skip remaining steps.
   b. If `loop_prs_14d == 0` AND `stale_ai_ready >= 1`:
      - Log: "Step 9L: LOOP STALLED — 0 loop PRs in 14 days, {stale_ai_ready} stale ai-ready issues"
      - Find oldest stale ai-ready issue.
      - Post comment via `mcp__github__add_issue_comment`:
        Body: "Loop stall diagnostic (Step 9L): 0 PRs opened from issue-to-pr-loop in last 14 days. AUTOPILOT_GH_TOKEN age: {token_age}d. Stale ai-ready issues: {stale_ai_ready}. Suggested checks: (1) GH Action 'issue-to-pr-loop' last run status, (2) Routine substrate logs in scripts/autonomy/, (3) token validity via `gh auth status`. Auto-generated by nightly Step 9L."
      - Add warning: "⚠ Step 9L: LOOP STALLED — investigate loop substrate"
   c. If `stale_ai_ready == 0`:
      - Log: "Step 9L: no stale ai-ready issues — diagnostic skipped"
5. Summary line: "Step 9L: loop {active|stalled|no_issues} ({loop_prs_14d} PRs in 14d, {stale_ai_ready} stale ai-ready)"
```

### Step 9E bonus (60d advisory, same commit)

In Step 9E block, add after age computation:

```markdown
   - If `token_age >= 60` AND `token_age < 76`:
     - Log: "⚠ ADVISORY: AUTOPILOT_GH_TOKEN at {token_age}d — alert threshold 76d. Schedule rotation within {76 - token_age}d."
```

### Commit
```
feat(nightly): add Step 9L ai-ready loop stall diagnostic + Step 9E 60d advisory
```

## What This Replaces
Active direction: Run 116 (branch run 116) proposed connector auth scan. This is complementary — targets loop runtime behavior, not connector code.

## Confidence
**HIGH** — live evidence of stall (3 issues >24h, valid token, 0 loop PRs visible); autonomous-executable channel proven across Steps 9C/9E/9F/9G/9I/9J/9K; `search_pull_requests` already used in Step 9J; comment pattern used in Step 9K; no production code changes; no architectural risk.

## Run 118 Mandate
1. Verify Step 9L (or 9M) fires in nightly-2026-09-03
2. Did it detect LOOP STALLED or LOOP ACTIVE?
3. If STALLED: did it post comment on #669?
4. Step 9E 60d advisory: did it log for token at ~61d?
5. sales_exact_email.ts: PRs #745+? If ≥2 more, escalate Idea 2 to run 118 winner.
6. os_tool_executions.py: stable (0 commits 4d+)? If yes: god class split candidate.
7. GH #684 SUPABASE_ACCESS_TOKEN: any human action?
