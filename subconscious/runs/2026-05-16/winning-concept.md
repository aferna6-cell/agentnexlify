# Winning Concept — 2026-05-16 (Run 19)

## Recommendation
Formally encode the Moratorium Escalation Protocol in `.claude/skills/nightly-commit-review/SKILL.md`
by adding the "## Moratorium Escalation Protocol" section and step 10A to the Scheduled Task Prompt
(per the implementation sketch already written in `subconscious/runs/2026-05-15-pm/winning-concept.md`),
completing run 18's governance mandate.

## Why This, Why Now

**Run 18 fired informally today — but only once.** The nightly review (commit `3467528`, 2026-05-16)
created GH issue #169 by improvising from governance.json context. That behavior is context-dependent:
it happened because today's session included the subconscious state prominently. Future nightly reviews
start fresh and may not repeat it. SKILL.md encoding makes the behavior mechanical: the step fires
regardless of session context, every night the moratorium is active.

**GH #169 is one event; we need a sustained loop.** The JS Silent Catch governance precedent
(Apr 27 → May 4, 5 runs, then implemented) shows sustained visibility drives implementation. One GH
issue is a notification. Daily GH comments showing updated pending ages ("run 7: now 23 days, ~15 min;
run 8: now 22 days, ~5 min...") create escalating pressure. The SKILL.md update converts a one-shot
alert to an escalation loop.

**Run 18 mandate is incomplete.** Run 18 winning-concept.md specified three steps: (1) add Moratorium
Escalation Protocol section to SKILL.md, (2) add step 10A to Scheduled Task Prompt, (3) verify via
manual nightly trigger. Only step 3's output was achieved informally. Steps 1-2 remain undone.
Run 19 completes them.

**10 minutes of effort; compounding return.** The implementation sketch is already fully written in
run 18 winning-concept.md. This is a copy-and-edit operation. Every night the moratorium persists,
the mechanism produces a GH comment update automatically — zero additional effort.

## Implementation Sketch

### File: `.claude/skills/nightly-commit-review/SKILL.md`

#### Step 1: Add "## Moratorium Escalation Protocol" section (~5 min)
Insert after `## Output Artifacts` block (after line 129), before `## Scheduled Task Prompt`:

```markdown
## Moratorium Escalation Protocol

After writing the nightly report, check moratorium state:

1. Read `subconscious/state/governance.json`
2. If `moratorium_config.moratorium_active == true`:
   a. Count `active_directions` where `status == "pending_approval"` → N_pending
   b. For each pending item, compute days since `date` field → find oldest_age_days
   c. If N_pending > 3 AND oldest_age_days > 14:
      - Search open GH issues for title containing "[subconscious] moratorium"
      - If NONE found: create new issue via mcp__github__create_issue (title/body below)
      - If one exists: add comment via mcp__github__add_issue_comment (status update)
   d. Add "## Moratorium Status" section to nightly report (see report structure update below)

### Moratorium Issue Template

**Title:** `[subconscious] Moratorium active: {N} pending items, oldest {X} days`

**Body:**
```
## AgentNexLiFy Subconscious — Moratorium Alert

Moratorium active. {N} subconscious recommendations pending approval or implementation.
New feature recommendations suppressed until cleared.

| Run | Title | Since | Days | Effort |
|-----|-------|-------|------|--------|
{rows from active_directions where status=pending_approval}

### Fastest path to exit moratorium
{list S-effort items with total minutes}
Implementation sketches: subconscious/runs/{latest-run}/winning-concept.md

### Governance note
While moratorium active, new recommendations are suppressed (except escalation meta-fixes).
Current winner: {active_directions[0].title}

Auto-generated by nightly-commit-review on {DATE}.
```

**Labels:** `subconscious`, `moratorium`

### Moratorium Comment Template (when issue exists)

**Body:**
```
**Moratorium Status Update — {DATE}**

| Run | Title | Days | Effort |
|-----|-------|------|--------|
{rows from active_directions where status=pending_approval, re-computed days}

Fastest exit: {total S-effort minutes} — implementation sketches in subconscious/runs/{latest}.
```
```

#### Step 2: Update Scheduled Task Prompt (~3 min)
After step 9 ("Write report to docs/dev-knowledge/nightly-reviews/YYYY-MM-DD.md"),
add step 9A:

```
9A. (Moratorium Escalation) Read subconscious/state/governance.json.
    If moratorium_config.moratorium_active == true:
      a. Count pending_approval items in active_directions → N_pending
      b. Find oldest pending item (by date field), compute days since today → oldest_age
      c. If N_pending > 3 AND oldest_age > 14:
         - Search GH issues: title contains "[subconscious] moratorium" AND state=open
         - None found: mcp__github__create_issue (title/body per Moratorium Issue Template)
         - Found: mcp__github__add_issue_comment (status update per Moratorium Comment Template)
      d. Append "## Moratorium Status" to report:
         - moratorium_active: true/false
         - N_pending, oldest_age_days
         - action_taken: "created issue #N" / "commented on #N" / "threshold not met"
    If moratorium_config.moratorium_active == false: skip; append "## Moratorium Status: CLEAR"
```

Also update the report structure (after `### Skipped` block):

```markdown
## Moratorium Status
[ACTIVE: N pending, oldest X days — action: created/commented #N]
OR
[CLEAR]
```

#### Step 3: Verify (~2 min)
After editing, manually confirm by reading the updated SKILL.md to check both sections
are present. No manual nightly trigger needed — next scheduled run (2:37 AM local)
will include step 9A.

### Total edit effort: ~10 minutes

---

## What This Replaces
Run 18's Automated Moratorium Escalation Hook recommendation (2026-05-15-pm). Run 18 produced
the winning-concept.md with the full implementation sketch but the SKILL.md was never updated.
GH issue #169 was created today (2026-05-16) by the nightly review via improvised behavior —
steps 1-2 still undone.

---

## After SKILL.md Updated — Next Run (Run 20)

If SKILL.md updated and nightly review adds comment to #169:
- Moratorium escalation mechanism is OPERATIONAL
- First post-mechanism run: recommend Idea 2 (ai-ready GH issues for issue-to-pr-loop)
  OR Widget Sync Guard (moratorium protocol: oldest S-effort)

If SKILL.md NOT updated by run 20:
- Moratorium mechanism is stalled at meta-layer
- Governance action: reduce max_pending_approvals 3→2 + create GH milestone
  (ideas 4+5 from this run's candidate list)

---

## Confidence
**HIGH** — Evidence: (1) SKILL.md missing Moratorium Escalation Protocol confirmed (read
directly, no section present); (2) GH #169 confirms mechanism works when triggered;
(3) implementation sketch pre-written and complete (run 18 winning-concept.md §Steps 1-2);
(4) zero mechanism uncertainty — SKILL.md edit is deterministic, nightly review reads SKILL.md;
(5) 10 min bounded effort; (6) debate: Idea 1 SURVIVES, Idea 2 WEAKENED, Idea 3 KILLED.
