# Winning Concept — 2026-05-15-pm (Run 18)

## Recommendation
Update `.claude/skills/nightly-commit-review/SKILL.md` to add a Moratorium Escalation
Protocol: when moratorium is active and the oldest pending item exceeds 14 days, the
nightly review creates a GitHub issue (or updates an existing one) via `mcp__github__create_issue`,
making the moratorium visible in GitHub where implementation decisions are made.

## Why This, Why Now

**Governance mandate fires.** Run 17 set an explicit boundary condition: if
`scripts/check-widget-sync.sh` is still absent at run 18, winner MUST be the Automated
Moratorium Escalation Hook. Widget Sync Guard is confirmed MISSING (5 consecutive nightly
reviews: May 11-15 all log "MISSING"). The 4-consecutive-run threshold is reached.

**Current notification channel is insufficient.** The subconscious system has produced
winning-concept.md files, commit messages, and governance.json updates for 21 days without
triggering implementation. These artifacts require the human to browse git history or the
subconscious/ directory. GitHub issues push to email notifications and surface in the Issues
tab — a different attention channel with higher conversion probability.

**The nightly review is already the right vehicle.** Three consecutive nightly reviews
(May 13-15) already include a "Subconscious Moratorium Status" prose section. We are
formalizing this into a GH issue creation step using `mcp__github__create_issue`, which
Claude Code has access to during nightly runs. No new infrastructure required.

**Precedent validates the mechanism.** JS Silent Catch Guard ran 5 consecutive moratorium
runs (April 27 → May 4) before implementation on May 5. The mechanism works — it
requires sustained, visible pressure. Adding GH-native escalation makes the pressure
visible in a second channel.

## Implementation Sketch

### Target file: `.claude/skills/nightly-commit-review/SKILL.md`

#### Step 1: Add "Moratorium Escalation Protocol" section (~10 min)
Insert between `## Output Artifacts` and `## Scheduled Task Prompt`:

```markdown
## Moratorium Escalation Protocol

After writing the nightly report, check moratorium state:

1. Read `subconscious/state/governance.json`
2. If `moratorium_config.moratorium_active == true`:
   a. Count `active_directions` where `status == "pending_approval"` → N_pending
   b. For each pending item, calculate days since `date` field → find oldest_age_days
   c. If N_pending > 3 AND oldest_age_days > 14:
      - Search open GH issues for title containing "[subconscious] moratorium"
      - If NONE found: create new issue via mcp__github__create_issue
      - If one exists: add comment via mcp__github__add_issue_comment
   d. Issue/comment format: see template below
3. Log action in report under `## Moratorium Status` section

### Moratorium Issue Template
**Title:** `[subconscious] Moratorium active: {N} pending items, oldest {X} days`
**Body:**
\`\`\`
## AgentNexLiFy Subconscious — Moratorium Alert

Moratorium has been active for {X} days. {N} subconscious recommendations are pending
approval or implementation. All are blocking new work recommendations until cleared.

| Run | Title | Since | Effort | Status |
|-----|-------|-------|--------|--------|
{rows from active_directions where status=pending_approval}

### Fastest path to exit moratorium
- Runs 7 + 8 + 14 total: ~40 minutes. Drops pending 4→1.
- Latest implementation sketch: subconscious/runs/2026-05-15-pm/winning-concept.md

### Governance note
While moratorium active, new recommendations are suppressed (except escalation meta-fixes).
Current winner: {active_directions[0].title}

Auto-generated by nightly-commit-review on {DATE}.
\`\`\`
**Labels:** `subconscious`, `moratorium`
```

#### Step 2: Update Scheduled Task Prompt (~5 min)
Add step 10A to the existing numbered sequence:

```
10A. (Moratorium Escalation) Read subconscious/state/governance.json.
     If moratorium_config.moratorium_active == true:
       a. Count pending_approval items in active_directions → N_pending
       b. Find oldest (by date) pending item, compute days since date → oldest_age
       c. If N_pending > 3 AND oldest_age > 14:
          - Search GH issues: label=moratorium AND state=open
          - None found: mcp__github__create_issue (title/body per Moratorium Issue Template)
          - Found: mcp__github__add_issue_comment (status update: current pending count + ages)
       d. Add "## Moratorium Status" to report: moratorium_active, N_pending, oldest_age,
          action_taken (created issue #N / commented on #N / no action — threshold not met)
```

#### Step 3: Verify (~5 min)
Trigger a manual nightly review (`bash scripts/daily/nightly-commit-review.sh`) and
confirm:
- Report includes "## Moratorium Status" section
- GH issue created (or comment added if issue exists) with correct pending table
- Action logged in ops/routines/logs/YYYY-MM-DD.md

### Total effort: ~20 minutes

---

## Bonus Steps (same sitting)

**Bonus A: Widget 3-Copy Sync Guard (run 7, day 21, ~15 min)**
Now demoted from winner to bonus — but still needs to happen.
- Create `scripts/check-widget-sync.sh` (script in run 17 winning-concept.md)
- `chmod +x scripts/check-widget-sync.sh`
- Wire into `scripts/hooks/pre-push` (8-line block, run 17 winning-concept.md §Step 2)
- Fix CLAUDE.md Invariant #4: "widget/ AND frontend/public/widget/" → add "AND landing-page-v2/widget/"
- Verify: `bash scripts/check-widget-sync.sh` → OK: all 3 widget copies are byte-identical
- Drops pending 4→3

**Bonus B: Wire check_project_invariants.py into pre-commit (run 8, day 20, ~5 min)**
- Add Check 10 block to `scripts/hooks/pre-commit` (8-line block, run 17 winning-concept.md §Bonus A)
- Script passes 6/6 checks (confirmed May 5)
- Drops pending 3→2

**Bonus C: Wire lead qualifier golden eval to CI (run 14, day 10, ~20 min)**
- Create `.github/workflows/lead-qualifier-eval.yml` (YAML in run 17 winning-concept.md §Bonus B)
- Add LEAD_QUALIFIER_AGENT_ID to GitHub Secrets
- Closes Issue #110
- Drops pending 2→1

**After all bonuses: pending = 1 (only run 4 remains — M-effort, requires deliberate sprint).**

---

## What This Replaces
Run 17's Widget 3-Copy Sync Guard recommendation (2026-05-15 AM). Governance boundary
condition mandated winner switch at run 18.

## After Moratorium Exits (pending ≤ 3)

1. **Zapier API key plan_status enforcement** (issue #107, 15+ days, ROI 2.5, HIGH security)
   Route via issue-to-pr-loop. Add `plan_status IN ('active','trialing')` filter in
   `backend/services/zapier_auth.py::_get_api_key_client`. First code fix post-moratorium.

2. **Email Sequences N+1** (GH #112, ROI 2.3, M-effort) — bulk `.in_()` queries.

3. **AI-to-Human Handoff v1** (run 4, 29+ days) — explicit-trigger-only, requires sprint
   allocation. Cannot be auto-implemented.

## Confidence
**HIGH** — Evidence: (1) governance mandate binding (run 17 boundary condition met);
(2) Widget Sync Guard MISSING confirmed May 15; (3) nightly review already produces
moratorium prose sections — adding GH issue creation is same mechanism, new output;
(4) mcp__github__create_issue is in Claude Code toolset (confirmed); (5) jq available
for governance.json parsing (confirmed); (6) JS Silent Catch precedent validates
sustained-pressure mechanism; (7) debate: all 3 alternatives KILLED with strong
independent objections; (8) implementation is ~20 min, bounded scope.
