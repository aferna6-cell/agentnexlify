# Winning Concept — 2026-07-25 (Run 101)

## Recommendation
Post a comprehensive unblock checklist on GH #500 ("GitHub Actions down repo-wide — spending limit hit") that packages all 4 credential actions in one place and quantifies what each unlocks.

## Why This, Why Now
GH Actions spending limit has been hit since 2026-07-20 (GH #500, OPEN, label: human-action-required). This single billing event is the root cause behind 4 open issues (#399 AUTOPILOT_GH_TOKEN, #403 ANTHROPIC_API_KEY missing from Actions, #536 migration 176 unblocked by manual credential provision, Step 9G can't trigger workflows). The morning digest describes the fix steps in its daily log file, but no autonomous session has posted a structured comment directly on GH #500 packaging all 4 actions together with verification steps and opportunity-cost framing. 30 ai-ready issues are stalled with ~60 engineering-hours queued. A single 15-minute human action unblocks everything.

## Implementation Sketch

Use `mcp__github__add_issue_comment` on GH #500 (repo: aferna6-cell/agentnexlify). Comment body:

```
## Subconscious Run 101 — GH Actions Unblock Checklist

**Impact of current block:** 30 ai-ready issues stalled (GH #399), KB autopopulate dark (GH #403), Step 9G workflow trigger non-functional, all health-check and autopilot workflows dead. Estimated ~60 engineering-hours queued behind this one fix.

### Step 1 — Restore Actions spending limit (5 min)
- GitHub → Settings → Billing and Plans → Usage and Limits → GitHub Actions spending limit
- Set to $5–$20 (enough to unblock current backlog; ≤$10 for normal monthly usage)
- Verify: Actions tab shows workflows queuing again

### Step 2 — Rotate AUTOPILOT_GH_TOKEN (5 min, closes #399)
- GitHub → Settings → Developer Settings → Personal Access Tokens (Classic)
- Create new PAT with scopes: `repo`, `workflow`, `issues:write`, `pull_requests:write`
- Repo → Settings → Secrets and Variables → Actions → AUTOPILOT_GH_TOKEN → update value
- Trigger manual run of `autopilot-issue-loop.yml` to verify

### Step 3 — Set ANTHROPIC_API_KEY in Actions secrets (2 min, closes #403)
- Repo → Settings → Secrets → Actions → New repository secret
- Name: `ANTHROPIC_API_KEY` — Value: same key Railway uses
- Trigger manual run of `kb-autopopulate.yml` to verify

### Step 4 — Optional but high-value (5 min)
- `VOYAGE_API_KEY` — enables vector embeddings in KB autopopulate (currently using FTS fallback)
- `SUPABASE_ACCESS_TOKEN` — enables KB supabase writes from Actions

### Verification
After Steps 1–3:
```
gh workflow run kb-autopopulate.yml -R aferna6-cell/agentnexlify
gh run list --workflow=kb-autopopulate.yml --limit=1
```
Expected: `conclusion: success` within 3–5 minutes.

---
*Generated by subconscious run 101 (2026-07-25) — total_runs=101*
```

## What This Replaces
Prior active direction was symptom-by-symptom commenting on GH #399 and GH #403 separately. This run packages all symptoms into one root-cause comment on GH #500 with a clear sequence and verification step.

## Confidence
**HIGH** — autonomous-executable via `mcp__github__add_issue_comment`, no code changes, no schema risk. Evidence: nightly-2026-07-25 confirms Actions still dead (only log commits in 24h window). Prior sessions have NOT posted on GH #500 directly (governance corrections show #399/#403 comments but not #500). Multiplier effect verified: GH #399 + GH #403 + Step 9G workflow + all scheduled workflows are all unblocked by this one billing action.
