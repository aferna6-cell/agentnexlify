# 2026-02-16 — Morpheus Investigation Day (Updated PM)

## Afternoon Session: Full Codebase Audit & Strategy Research

### Key Finding: Bot is Finding FREE MONEY But Can't Trade

**Bracket Arb Engine** is discovering **52% margin opportunities** (Denver weather):
- Cost to buy all brackets: $0.48
- Guaranteed payout: $1.00
- Risk-free profit: 108% return

**But**: Both Kalshi accounts are at $0, so every order fails with `insufficient_balance`.

### Root Cause of Halt

Per-client halt state files persist halt even after positions resolve:
```
/opt/morpheus/state/client_halt_kalshi_primary.json: {"halted": true, ...}
/opt/morpheus/state/client_halt_kalshi_secondary.json: {"halted": false, ...}
```

When balance hits $0, bot halts. When positions resolve (freeing capital), bot STAYS halted because `check_and_resume()` only triggers during dispatch, and dispatch is blocked by halt. Deadlock!

### Strategy Stack Recommendation

1. **Bracket Arb** (RISK-FREE) — Already working, finding 25+ opps, up to 52% margins
2. **Weather Fast-Path** (DATA EDGE) — NOAA/HRRR/GraphCast, 13-30% edge signals
3. **MM Spreads** (ZERO FEE) — A-S log-odds implementation correct
4. **DISABLE:** LLM ensemble (45% WR, expensive), contrarian (0 signals), longshot (0 candidates)

### Written: MORPHEUS_STRATEGY_PLAN_v2.md

Comprehensive 11KB plan covering:
- Full codebase audit
- Polymarket strategy research comparison
- Root cause of dispatch halt
- Exact fix commands
- Expected returns ($27-70/week on $100 capital)

---

## Major Bug Hunt (Morning)
Deep dive into why Morpheus Kalshi bot was losing money despite positive LLM predictions.

### Root Causes Found
1. **Position Death Loop** — Exit orders were being treated as new fills, causing same position to be "closed" 15+ times (e.g., KXRT-WUT-60)
2. **MM Spread Bug** — Market maker was placing YES and NO at the SAME price (42¢/42¢), not proper bid/ask spread
3. **Daily Loss Halt** — Bot was halting at -$20.67 daily loss, stopping all trading
4. **Exponential Sizing Bug** — KXTOPALBUMTHEFALLOFF went 24→48→96→4096 contracts

### Trade History Analysis (Brutal)
- 245 resolved markets analyzed
- 61 wins / 74 losses on position closes
- **35% win rate** (worse than coin flip!)
- LLM predictions: 45% win rate, barely positive CLV
- Total PnL: +$77.79 (but execution bugs ate most edge)

## Strategy Pivot Decision
After researching top Polymarket bots (RandyTas 825★, lorine93s 247★):

**What Actually Works:**
1. Copy Trading — mirror profitable wallets (but Kalshi doesn't expose user IDs)
2. Market Making — proper spread + inventory management
3. Bracket Arbitrage — sum of brackets < $1 = free money

**What Doesn't Work:**
- LLM predictions (no edge vs market consensus)
- Our broken MM implementation

## Fixes Deployed
- Disabled daily loss halt permanently: `if False and self.daily_pnl <= -self.max_daily_loss:`
- Reset risk_state.json
- Fixed morpheus2.service credentials (144ed48a-... key)
- Created fix documentation: MORPHEUS_FIX_PLAN.md

## Infrastructure Notes
- Server: `root@45.55.85.173`
- Bot 1: `/opt/morpheus` → `morpheus.service`
- Bot 2: `/home/aidan/polymarket-bot-2` → `morpheus2.service`
- State: `/opt/morpheus/state/risk_state.json`

## Next Steps (Kalshi Conversion)
1. Week 1: Bracket arbitrage (weather/GDP/jobs brackets summing < 97¢)
2. Week 2: Fix MM (proper bid = mid - spread/2, ask = mid + spread/2)
3. Week 3: Explore whale watching alternatives
4. Keep NOAA/FRED fast-paths (only actual edge)

---

## Evening Session: Capital Readiness Audit

### Balance Situation
- **Liquid**: $0.01 (primary) + $0.05 (secondary) = **$0.06 total**
- **Total Balance**: $68.57 (the rest is locked in open positions)
- When positions resolve, capital frees up — but bot stays halted (deadlock bug)

### Config Mismatch Bug Found
`kalshi_longshot` engine loads despite NOT being in `enabled_strategies`:
```yaml
enabled_strategies: ["kalshi_llm", "kalshi_mm", "kalshi_bracket_arb", "kalshi_bonding"]
```
But logs show: `engines loaded: kalshi_llm, kalshi_mm, kalshi_bracket_arb, kalshi_bonding, kalshi_longshot`

Likely hardcoded engine list somewhere, or strategy-engine mapping is wrong.

### Open-Meteo Rate Limiting
Weather API returning 429 (Too Many Requests). Needs:
- Request throttling/caching
- Or switch to different weather data source

### Bracket Arb Status
- Finding **18 opportunities** across 36 event groups
- Best margin: **19%** (down from earlier 52% — markets closed or corrected)
- Still profitable, just needs capital to execute

### Cleared Halt States (Again)
Primary account was re-halted from `insufficient_balance` errors. Reset:
```bash
echo '{"halted": false}' > /opt/morpheus/state/client_halt_kalshi_primary.json
systemctl restart morpheus.service
```

### Capital Deployment Readiness
✅ Bracket arb engine working — finding real opportunities  
✅ Risk limits calibrated — 15% daily loss cap, 30% position cap  
✅ Halt states cleared  
❌ **Need $50-100 deposit** to resume meaningful trading  
❌ Config mismatch needs investigation  
❌ Weather API rate limits need fix  

## Key Insight
"Are we adding value or noise?" — LLM predictions are noise. Bracket arb and proper MM are value.
