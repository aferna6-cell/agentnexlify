# Design References

Extracted design-token artifacts from reference URLs. Generated by `.claude/skills/ui-reference/SKILL.md`.

Each artifact represents inspiration — NOT a verbatim clone. See the skill for anti-patterns (no logos/copy/1:1 layouts).

Naming: `{source-slug}-{YYYY-MM-DD}.md`
