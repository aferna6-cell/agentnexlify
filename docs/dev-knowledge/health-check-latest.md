# Health Check Report — 2026-04-08

Generated by hard debugging session.

---

## Critical

### 1. Sentry Double Initialization (FIXED)

**File:** `backend/main.py`
**Issue:** `sentry_sdk.init()` called twice — once at lines 24-31 (basic, no integrations) and again at lines 112-122 (with FastAPI/Starlette integrations). The second call silently overwrites the first. The unconditional `import sentry_sdk` at line 13 also makes sentry_sdk a hard dependency even when `sentry_dsn` is not set.
**Fix applied:** Removed the first redundant init block. Now Sentry initializes once with proper integrations when `sentry_dsn` is configured.

### 2. Duplicate Migration Numbers (KNOWN)

**Files:**
- `005_appointments.sql` / `005_automation_sequences.sql`
- `007_google_calendar_integration.sql` / `007_team_members.sql` / `007_webhooks.sql`

**Impact:** Low — all migrations are already applied. Supabase tracks by name not number. But it creates confusion when reading migration history.
**Recommendation:** Do not renumber (already applied). Document as legacy. Next migration is 097.

---

## Warning

### 3. `lead_stage_change` Event Name May Confuse

**Files:** `backend/services/automation_engine.py:31`, `backend/routers/sequences.py:579`, `backend/models/schemas.py:671`
**Issue:** The trigger event is named `lead_stage_change` but the actual DB column is `status`. The event name is fine as an application-level concept, but could mislead new developers into thinking a `lead_stage` column exists.
**Status:** Not a bug — the queries correctly use `status` column. The event name is just a label.

---

## Info (All Clear)

| Check | Result |
|-------|--------|
| `from __future__ import annotations` in routers | None found (only in CONTEXT.md docs) |
| Bare `except: pass` patterns | None — all exception handlers log properly |
| Dead imports (nonexistent modules) | None found |
| Unregistered routers | All 67 router files accounted for. `widget_booking.py` and `widget_helpers.py` are helper modules (no router object) — correctly excluded |
| Leads table: `client_id` usage | All direct queries use `client_id`. `tenant_scope.py` helpers handle mapping |
| Leads table: `status` vs `lead_stage` | No queries use `lead_stage` as a column. Only `lead_stage_change` event name exists |
| Conversations table: `client_id` usage | All queries use `client_id` correctly |
| Widget JS sync | `widget/agentnexlify-widget.js` identical to `frontend/public/widget/agentnexlify-widget.js` |
| Frontend build | Clean build, no warnings, 3.71s |
| Python syntax (all files) | All `.py` files compile without errors |
| Router import resolution | All 67 router modules import successfully |
| Hardcoded secrets | None found in backend or frontend |
| CORS configuration | `allow_origins=["*"]` — correct for embeddable widget. Auth via JWT header, `allow_credentials=False` prevents CSRF |
| Async/sync route mismatches | None found |
| Migration numbering (recent) | 077-096 sequential, no gaps or duplicates |

---

## Summary

**1 critical issue fixed** (Sentry double-init), **1 known legacy issue** (duplicate migration numbers 005/007), **0 new bugs found**. Codebase is in good shape.
