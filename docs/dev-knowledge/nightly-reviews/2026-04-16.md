# Nightly Review — 2026-04-16

**STATUS: PARTIAL — dirty tree at 2:37 AM, re-ran review on user request**

## Commits reviewed (last 24h, no-merges)

Production code commits (non-doc, non-plan):
- `2ef7d07` feat(leads): enrichment_source column + lead quality stat bar
- `c9558dd` feat(lead-parser): Phase 4 — UI toggle for smart lead enrichment
- `8b2b20f` feat(lead-parser): Phase 2+3 — background structured enrichment + tests
- `e2a22a6` feat(migrations): 103 — widget_configs.enable_structured_lead_parser flag
- `dac41b9` test(lead-parser): standalone smoke runner — 10/10 pass

Docs/plans/skills commits (not reviewed — low risk):
- `e173184` docs(schema-log): log migrations 103+104+105
- `c4ed1a2` docs(skills): v1.1.0 encode lead-enrichment learnings
- `c771a2e` docs(lead-parser): resolve MTOptions UUID in rollout audit
- `5452b08` feat(lead-parser): Phase 5 infrastructure — audit + migration 104 pre-written

## Critical invariants check

All passed:
- No `from __future__ import annotations` in any new FastAPI files ✓
- `client_id` used via `tenant_select`/`tenant_update` helpers ✓
- `areas_of_interest` column name used correctly (not `service_interest`) ✓
- `status` used for lead status ✓
- Migrations in `migrations/` only ✓
- No secrets committed ✓
- Widget files not touched ✓

## Findings

### Fixed autonomously (1)

- **[LOW]** `backend/routers/widget_helpers.py:1472` — `async def _enrich_lead_from_message` had zero `await` expressions and called only sync helpers (`extract_structured`, `tenant_select`, `tenant_update`, `log_activity`). Starlette's BackgroundTasks awaits async background tasks directly in the event loop; sync tasks get dispatched to a thread pool. With `async def`, the managed-agent HTTP poll (`run_until_idle`) blocked the event loop worker for its full duration. Fixed by changing to `def`.
  → commit `95db5de` `fix(nightly): async-def→def for _enrich_lead_from_message [auto-nightly-2026-04-16]`

### Issues opened (0)

`gh` CLI unavailable in this environment. No MEDIUM/HIGH findings requiring issue creation.

### Skipped (FORBIDDEN paths)

- `migrations/103_widget_configs_enable_structured_lead_parser.sql` — not reviewed (migration path)
- `migrations/104_widget_configs_structured_lead_parser_default_true.sql` — not reviewed (migration path)
- `migrations/105_leads_enrichment_source.sql` — not reviewed (migration path)

## Other observations (not actionable autonomously)

- **Lead quality stat bar** (`2ef7d07` LeadsPage.jsx): `completionPct` and `aiEnrichedCount` calculated from current page/filter of leads, not total DB count. Could mislead when filters are active. UX-only concern, not a bug.
- **`_extract_lead_info` eager evaluation** (`8b2b20f` widget_chat.py:963): `_extract_lead_info(req.message)` is called in the main request handler before `background_tasks.add_task`. Called inside the `if widget.get("enable_structured_lead_parser"):` gate, so only runs when flag is on. Regex-only, fast — not a real concern.

## Next action

All clear — 1 LOW fix applied. No MEDIUM/HIGH findings. Migrations await human apply (103 applied per schema-log, 104 pending, 105 applied).
