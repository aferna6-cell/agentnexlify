# Skill Discovery Report — 2026-06-01

Generated from: `git log --since="7 days ago"` (2026-05-26 → 2026-06-01)  
Commits analyzed: 47 commits — Agent OS rehaul (Groups A+B+C), PostgREST fix sweep, nightly autonomous runs 36–44, morning digests.

---

## Summary

This was a high-signal week dominated by the Agent OS rehaul (#188). Two major feature patterns were applied 3 times each in a single session: BYO third-party integration and outbound mirror channel. A third pattern — codebase-wide API fix — appeared once but was sufficiently impactful (14 call sites fixed atomically) to codify. One prior candidate (`pre-commit-guard-add` from 2026-05-18) was re-observed and is escalated.

Already-existing skills that shipped this week: `god-class-splitter`, `post-split-test-repair`. No re-proposal needed.

---

## Proposed Skills

### 1. `byo-integration`

**Trigger condition:** User says "add `<provider>` integration", "wire up `<provider>` BYO", "tenant-connected `<provider>`", or when `os_actions/<action>.py` needs a second runtime dispatch branch for a new external service.

**Steps it would automate:**

1. Create `backend/services/<provider>_tenant.py`: CRUD against `integrations` table for `provider='<name>'`, OAuth helpers (`build_authorization_url`, `exchange_code`, `_refresh`, `get_access_token` with 60s skew margin + token rotation), `is_connected()`, provider API call (send/upsert/fetch). Never raises — fold all exceptions to `{success: False, detail}` shape.
2. Add 4 OAuth endpoints to `backend/routers/integrations.py` (mirror existing Google pattern): `GET /api/v1/integrations/<provider>/auth`, `GET /<provider>/callback`, `GET /<provider>/status`, `DELETE /<provider>`. JWT state token (10-min TTL) binds `tenant_id` across OAuth round-trip. Profile fetch best-effort only — connection succeeds if profile call fails.
3. Add `<provider>_client_id`, `<provider>_client_secret`, `<provider>_redirect_uri` to `backend/config.py`. Return 503 from `/auth` if credentials not configured.
4. Update `backend/services/os_actions/<action>.py`: add `_pick_provider(client_id)` branching on `is_connected`, new BYO handler, existing path as fallback. `request_payload` carries `provider` tag for audit.
5. Add `<Provider>Section` component to `frontend/src/pages/IntegrationsPage.jsx`: Connect button, status/metadata display when connected, disconnect flow. Handle `?<provider>=connected` URL param toast.
6. Add 3 helpers to `frontend/src/utils/api/integrations.js`: `fetch<Provider>Status`, `start<Provider>Auth`, `disconnect<Provider>`.
7. Write tests: 8 OAuth route tests (happy path, auth, missing-refresh-token rejection, tampered state, connected/disconnected status, disconnect) + 4 action dispatch tests (BYO success, BYO failure, platform fallback, `is_connected` exception safe-fallback).
8. Verify: `pytest backend/tests/ -q` passes, `cd frontend && npm run build` clean.

**Evidence:**
- `937b4c5` (2026-05-27): BYO Twilio — `twilio_tenant.py` + `sms.send` dispatch + 4 tests
- `de7223b` (2026-05-27): BYO HubSpot — `hubspot_tenant.py` + `crm.contact_upsert` dispatch; commit explicitly says "mirrors the BYO Twilio SMS pattern from chunk 10"
- `44e92a9` + `c339bb2` + `9a46fc9` (2026-05-27): M365 Calendar — OAuth routes + frontend section + runtime dispatch; all 3 commits in one session
- All three integrations use identical file topology: `<provider>_tenant.py` + 4 routes + 3 API helpers + Connect section + dispatch branch + 8+4 tests

**Estimated time saved:** 45–90 min per integration. Three integrations were built back-to-back in a single session using the same steps. A skill reduces first-attempt errors (especially: missing offline_access scope → refresh_token silently dies; `is_connected` raising uncaught on DB failure; Connect button missing disconnect flow).

---

### 2. `outbound-mirror-channel`

**Trigger condition:** User says "mirror OS replies to `<channel>`", "add `<channel>` outbound", or when a new inbound source (WhatsApp, Instagram DM, etc.) is wired but the reply path is missing.

**Steps it would automate:**

1. Confirm (or create) channel send primitive in the channel's service module. Function shape: async, never raises, returns `{success, detail}`. For existing services (Twilio, Resend), add a dedicated `send_<channel>_reply()` that bypasses any marketing rate limiting / wrapper.
2. Add `async _mirror_<channel>()` to `backend/services/os_outbound_mirror.py`. Required guards:
   - missing recipient → `skipped:no_session`
   - empty / whitespace content → `skipped:empty_content`
   - service failure → `error:<channel>_send:<detail>`
3. Add branch to `mirror_assistant_message()` dispatcher keyed on `source_channel`.
4. If the channel needs `source_thread_id` or `source_metadata` fields, expose them from `backend/services/os_thread_runner.py`.
5. Write `TestOutboundMirror<Channel>` test class (7 tests minimum): success path, variant metadata key (legacy field name), missing recipient skip, empty content skip (3 whitespace variants if applicable), no-credentials error, service exception caught.
6. Verify: `pytest tests/test_agent_os.py -q` passes.

**Evidence:**
- `e6f4495` (2026-05-27): SMS mirror — `_mirror_sms`, BYO/platform fallback, 7 tests
- `b3bfb0b` (2026-05-27): Email mirror — `send_email_reply()`, `_mirror_email`, In-Reply-To threading, 7 tests
- `e8de3bc` (2026-05-27): Facebook mirror — `facebook_messenger.py`, `_mirror_facebook`, PSID from `source_metadata`, 7 tests; commit: "Module docstring updated: phase-2 outbound now complete for SMS + email + Facebook"
- All three written same session; same guard set; same test count; identical `{success, detail}` contract on send functions

**Estimated time saved:** 20–40 min per channel. The 7-test template is the key value — each channel fix required inventing guard test cases from scratch. Skill encodes the guard set upfront, preventing silent skip bugs.

---

### 3. `codebase-api-fix`

**Trigger condition:** A bug is fixed in one call site AND the commit notes "N other call sites", OR `grep` reveals the same broken pattern repeated across the codebase (>3 occurrences).

**Steps it would automate:**

1. On finding the fix in commit N: immediately grep for all instances of the broken pattern across `backend/` and `tests/`.
   ```bash
   grep -rn "<broken_pattern>" backend/ tests/ | grep -v __pycache__
   ```
2. Record the full list (file + line) in the commit message under "Out of scope:".
3. Open a follow-up PR in the same session (don't defer): apply the mechanical substitution to all remaining call sites.
4. For each test file in the list: check if the mock simulates the broken path. If yes, update the mock (Rule 10 exception: contract changed — document it).
5. Run the full test suite to confirm nothing regressed.
6. Commit as one atomic PR: `fix(<scope>): <pattern> cleanup — N call sites`.

**Evidence:**
- `2a2ffe9` (2026-05-27): Fixed `.not_.is_()` → `.filter()` in `os_inbound.py`; commit explicitly documents "14 other call sites…tracked for dedicated cleanup PR"
- `6126397` (2026-05-27): Cleanup PR that swept all 14 sites (11 files) in one auto-commit
- `bca2082` (2026-05-27): Follow-up to update mocks that simulated the broken API chain

**Estimated time saved:** 20–30 min per sweep. The pattern already happened correctly — this skill codifies the discipline so the sweep is never deferred to a later PR (where it may stay "tracked" for weeks).

---

## Rejected Candidates

### Worker action wiring (`fix(os): wire <worker> to <action>`)
Four commits (`349bf25`, `23d7fe0`, `6c32246`, `2ac4f91`) all added `action_type` to a `WorkerResult` or wired a worker channel. Each was 1–5 lines. Too small to warrant a skill; would be better as a pre-flight checklist item in the `os-actions` conventions doc or a note in `workflow-orchestration.md`. Flag for doc update.

### CI workflow repair (`3ed0d3a`, `efabd0c`)
Disabled broken pr-check + regenerated agentshield baseline. One occurrence; highly context-specific to the Linux runner vs. macOS baseline mismatch. Too rare and too specific to codify.

### Subconscious / nightly review / morning digest
Automated loops already captured in existing skills. Runs 36–44 this week are normal operation, not a new pattern.

---

## Prior Candidates Re-Observed

### `pre-commit-guard-add` (first proposed 2026-05-18)
Re-observed twice this week:
- `061582c` (2026-05-29): billing-constant guard Check 11 added to `scripts/hooks/pre-commit`
- `4226ef4` (2026-06-01): nightly autonomous scope extended to cover pre-commit bash additions

The pattern is: identify invariant → write 10-line bash check → add WARNING-mode block to `scripts/hooks/pre-commit` → test it fires correctly. Was proposed in 2026-05-18 report but not created. Escalating to **strong create candidate** — 3 total occurrences across two weeks.

---

## Existing Skill Updates

### `migration-workflow` — Add "mark APPLIED in schema-log" step
`2a2ffe9` shows the pattern clearly: after `mcp__supabase__apply_migration` succeeds, flip the migration status in `docs/dev-knowledge/schema-log.md` from PENDING to APPLIED in the same commit. The skill's Step 5 should explicitly include: update `schema-log.md` status + record columns and indexes verified live. Currently this step is implied but not stated.

### `byo-integration` (new, cross-ref) → `migration-workflow`
When a new integration requires a new table (e.g., migration 130 `os_outbound_log` was part of Group C), the BYO integration skill should gate on `migration-workflow` first. Add this cross-ref once the skill is created.

### `nightly-commit-review` — Already self-updated
`4226ef4` (2026-06-01): skill was autonomously extended to include pre-commit bash additions in LOW autonomous scope. No human action needed — noted for awareness.

### `god-class-splitter` — Already updated
`ccf0c8e` (2026-05-30): cross-ref to `post-split-test-repair` added at Step 10.5. No further update needed.
