# Case-Study Template (rubric 8.5)

Fill-in-the-blanks structure for the first design-partner case study. Replace every
`{{placeholder}}` with real numbers before publishing.

> NOT FOR PUBLICATION YET. This template needs three things before it goes live:
> (1) written consent from the partner ([[MTOptions]] is the top beta tester) to use
> their name, logo, and numbers; (2) real metrics pulled from their dashboard, not
> estimates; (3) owner sign-off on the final copy. Do not publish placeholder or
> invented numbers. See `audits/audit-launch-readiness-2026-06-23.md` item 8.5.

Voice: direct, evidence-first, numbers over adjectives. No marketing buzzwords.
See `brain/Preferences/Builder Writing Voice.md`.

---

## Title

`{{business_name}} books {{appts_booked}} appointments in 30 days with an AI front desk`

(Title should state one concrete result. Numbers, not adjectives.)

---

## The business

- Name: `{{business_name}}`
- Industry: `{{vertical}}` (e.g. salon, HVAC, dental)
- Location: `{{city}}, {{state}}`
- Size: `{{team_size}}` staff
- Logo asset: `{{logo_file_path}}` (partner-supplied; needs consent)

---

## The problem

What was leaking before AgentNexLiFy. Two to three sentences, concrete.

- `{{problem_statement}}` (e.g. "Missed roughly {{missed_calls_per_week}} after-hours
  calls a week. Each one was a customer who booked elsewhere.")

---

## The setup

How fast they went live, what plan, what it replaced.

- Plan: `{{plan_name}}` (chatbot $19.99/mo or agent_os $99.99/mo)
- Time to live: `{{time_to_live}}` (e.g. "under 10 minutes")
- Replaced: `{{what_it_replaced}}` (e.g. "voicemail and a missed-call list")

---

## 30-day results

Pull these from the partner's dashboard. Every number must be real.

| Metric | Result |
|--------|--------|
| Leads captured | `{{leads_captured}}` |
| Appointments booked | `{{appts_booked}}` |
| Hours saved (front-desk time) | `{{hours_saved}}` |
| Pipeline value influenced | `${{pipeline_value}}` |
| After-hours leads captured | `{{after_hours_leads}}` |

(Use only the rows you have real data for. Delete rows you can't back with numbers.)

---

## In their words

> "`{{partner_quote}}`"
>
> - `{{owner_name}}`, `{{owner_title}}`, `{{business_name}}`

(Quote must be a real statement the partner approved in writing.)

---

## Why it worked

One short paragraph tying the result back to the product. Concrete mechanism, not hype.

- `{{mechanism}}` (e.g. "The AI answered every after-hours call, captured the
  contact, and booked the slot before the customer shopped a competitor.")

---

## Publishing checklist

- [ ] Written consent from the partner for name, logo, and numbers
- [ ] Every metric pulled from the real dashboard (no estimates)
- [ ] Quote approved in writing by the named person
- [ ] Logo asset supplied by the partner
- [ ] Owner sign-off on final copy
- [ ] Pricing shown is current (chatbot $19.99 / agent_os $99.99)
- [ ] No placeholder text remains
